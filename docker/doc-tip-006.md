# 🐳 Docker Tip #006 — Remove Container

Remove container.

```bash
docker rm container_id
```

Remove stopped containers:

```bash
docker container prune
```

## Why It's Useful

Clean unused containers.
