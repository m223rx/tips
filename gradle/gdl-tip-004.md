# ⚡ Gradle Tip #004 — Add Dependency

Add dependencies in `build.gradle`.

```gradle
dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
}
```

## Why It's Useful

Manage libraries easily.
