# 🔥 Firebase Tip #005 — Firebase Authentication

Initialize Auth.

```javascript
import { getAuth } from "firebase/auth";

const auth = getAuth();
```

## Why It's Useful

User authentication support.
