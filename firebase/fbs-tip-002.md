# 🔥 Firebase Tip #002 — Use Firestore

Initialize Firestore.

```javascript
import { getFirestore } from "firebase/firestore";

const db = getFirestore(app);
```

## Why It's Useful

Store and retrieve data easily.
