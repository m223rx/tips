# 🐧 Linux Tip #007 — Move or Rename Files

Move or rename files using `mv`.

```bash
mv file.txt newfile.txt
```

Move file:

```bash
mv file.txt folder/
```

## Why It's Useful

Rename and move with one command.
