# 🐧 Linux Tip #002 — Show Directory Tree

View folder structure easily.

```bash
tree
```

If not installed:

```bash
sudo apt install tree
```

## Why It's Useful

Quickly visualize project structure.
