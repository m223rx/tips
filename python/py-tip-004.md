# 🐍 Python Tip #004 — Join Strings

Use `join()` to combine list elements into a string.

## Example

```python
words = ["Python", "is", "awesome"]

sentence = " ".join(words)
print(sentence)
```

## Output

```
Python is awesome
```

## Why It's Useful

Much faster than concatenating strings.
