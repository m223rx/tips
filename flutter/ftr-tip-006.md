# 🐦 Flutter Tip #006 — Use `Navigator`

Navigate between screens.

```dart
Navigator.push(
  context,
  MaterialPageRoute(builder: (context) => SecondScreen()),
);
```

## Why It's Useful

Basic navigation in Flutter apps.
