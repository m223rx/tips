# 🖥️ Bash Tip #008 — Find Text in File

Search text inside files.

```bash
grep "m223rx" file.txt
```

Search recursively:

```bash
grep -r "m223rx" .
```

## Why It's Useful

Powerful text searching.
