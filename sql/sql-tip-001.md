# 🗄️ SQL Tip #001 — Select All Columns

Select all columns from a table.

```sql
SELECT * FROM users;
```

## Why It's Useful

Quickly view all data in a table.

⚠️ Avoid using `SELECT *` in production for better performance.
