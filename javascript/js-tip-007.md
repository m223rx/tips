# ⚡ JavaScript Tip #007 — Array Map

Transform arrays easily with `map()`.

```javascript id="js-map"
const numbers = [1, 2, 3];
const squares = numbers.map(x => x * x);

console.log(squares);
```

**Output:**

```
[1, 4, 9]
```
