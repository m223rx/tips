# ⚡ JavaScript Tip #008 — Array Filter

Filter arrays based on a condition.

```javascript id="js-filter"
const numbers = [1, 2, 3, 4, 5];
const even = numbers.filter(x => x % 2 === 0);

console.log(even);
```

**Output:**

```
[2, 4]
```
