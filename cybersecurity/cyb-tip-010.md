# 🔐 Cybersecurity Tip #010 — Scan Open Ports

Scan ports locally:

```bash
nmap localhost
```

Install nmap:

```bash
sudo apt install nmap
```

## Why It's Useful

Find exposed services and vulnerabilities.
