# 🔐 Cybersecurity Tip #006 — Use Firewall

Enable firewall:

```bash
sudo ufw enable
```

Allow SSH:

```bash
sudo ufw allow ssh
```

Check status:

```bash
sudo ufw status
```

## Why It's Useful

Blocks unauthorized access.
