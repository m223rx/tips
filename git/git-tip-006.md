# 🌿 Git Tip #006 — Create Branch

Create new branch.

```bash
git branch feature
```

Switch to branch:

```bash
git checkout feature
```

Or shortcut:

```bash
git checkout -b feature
```

## Why It's Useful

Work on features safely.
