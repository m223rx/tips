# ⚙️ DevOps Tip #002 — Use Environment Variables

Store configuration in environment variables.

```bash
export APP_ENV=production
export DB_HOST=localhost
```

## Why It's Useful

Keeps secrets and configs separate from code.
