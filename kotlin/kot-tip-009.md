# 🟣 Kotlin Tip #009 — Use Collections

Create lists easily.

```kotlin
val list = listOf("a", "b", "c")
```

Mutable:

```kotlin
val list = mutableListOf("a", "b")
```

## Why It's Useful

Simplifies collection handling.
