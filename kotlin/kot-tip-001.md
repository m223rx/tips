# 🟣 Kotlin Tip #001 — Use `val` Instead of `var`

Prefer immutable variables.

```kotlin
val name = "m223rx"
```

Instead of:

```kotlin
var name = "m223rx"
```

## Why It's Useful

Immutable variables make your code safer and easier to debug.
