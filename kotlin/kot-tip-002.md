# 🟣 Kotlin Tip #002 — Use String Templates

Use `$` for cleaner string formatting.

```kotlin
val name = "m223rx"
println("Hello $name")
```

Instead of:

```kotlin
println("Hello " + name)
```

## Why It's Useful

Cleaner and more readable code.
